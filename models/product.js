class Product {
  constructor(id, userId, title, imgUrl, description, price) {
    this.id = id;
    this.userId = userId;
    this.title = title;
    this.imgUrl = imgUrl;
    this.description = description;
    this.price = price;
  }
}

export default Product;
